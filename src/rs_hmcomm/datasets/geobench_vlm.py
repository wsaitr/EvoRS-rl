def load_geobench_vlm_placeholder(*args, **kwargs):
    raise NotImplementedError(
        "Pin the official GEO-Bench-VLM dataset revision and map its task-specific schemas "
        "inside this adapter after downloading the official release."
    )
